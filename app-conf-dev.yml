port: 8080

data:
  database:
    user: "${DB_USER|postgres}"
    pass: "${DB_PASS|postgres}"
    name: "${DB_NAME|go-web-templates-dev}"
    host: "${DB_HOST|database}"
    port: "${DB_PORT|5432}"

  cache:
    host: "${CACHE_HOST|cache}"
    pass: "${CACHE_PASS|redis}"
    port: "${CACHE_PORT|6379}"
